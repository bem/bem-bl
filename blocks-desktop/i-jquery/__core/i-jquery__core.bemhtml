block i-jquery, elem core, default: {
    return apply(
        this._mode = '',
        this.ctx = {
            block: 'b-page',
            elem: 'js',
            url: '//yandex.st/jquery/1.7.2/jquery.min.js'
        }
    );
}
