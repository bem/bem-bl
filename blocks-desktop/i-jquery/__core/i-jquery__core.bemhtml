block i-jquery, elem core, default: {
    return apply(
        this._mode = '',
        this.ctx = {
            block: 'b-page',
            elem: 'js',
            url: '//yandex.st/jquery/1.8.3/jquery.min.js'
        }
    );
}
