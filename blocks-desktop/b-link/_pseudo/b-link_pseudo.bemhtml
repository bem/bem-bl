block('b-link').match(this.mods && this.mods.pseudo).match(!this.elem)(

    tag()(this.ctx.url? 'a' : 'span'),

    js()(true),

    match(!this.ctx.url)(
        attrs()({})
    ),

    match(this.ctx._wrap && !this.mods.inner)(
        applyCtx({
            elem: 'inner',
            content: this.ctx.content,
            _wrap: true
        })
    )

)
