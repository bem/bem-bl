block b-link {

    tag: 'a'

    attrs: {

        var ctx = this.ctx,
            a = { href: ctx.url };

        ctx.target && (a.target = ctx.target);
        ctx.counter && (a.onmousedown = ctx.counter);
        ctx.title && (a.title = ctx.title);

        return a;

    }

}
